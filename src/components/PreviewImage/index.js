import { StyledPreviewImage } from './PreviewImage.styled'

export default function PreviewImage(src, alt, width, height) {
  return <StyledPreviewImage src={src} alt={alt} width={width} height={height} />
}
