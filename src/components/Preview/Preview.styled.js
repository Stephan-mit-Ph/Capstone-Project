import styled from 'styled-components'

export const StyledAbout = styled.p`
  font-family: var(--font-family);
  font-size: 1rem;
    font-weight: 400;
  text-align: left;
  max-width: 600px;
  padding: 1.5rem;
  text-align: center;
`
